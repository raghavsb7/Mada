# Flask Configuration
# Set to 'true' only for development, 'false' for production
FLASK_DEBUG=false

# Secret key for Flask sessions (change this in production!)
SECRET_KEY=change-this-to-a-random-secret-key-in-production

# Database URI (default is SQLite)
DATABASE_URI=sqlite:///mada.db
